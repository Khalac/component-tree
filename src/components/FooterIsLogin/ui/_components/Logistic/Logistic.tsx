const Logistic = () => {
  return (
    <div>
      <div>
        Logistic
        {Array(2)
          .fill(0)
          .map(() => {
            return <div>Logistic image</div>;
          })}
      </div>
    </div>
  );
};

export default Logistic;
